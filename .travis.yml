dist: trusty
sudo: required
matrix:
  include:
      - language: python
        python:
            - "3.6"
        install:
            - pip install -r requirements.txt
      - language:
        node_js:
            - 11.15.0
        install:
            - npm install
